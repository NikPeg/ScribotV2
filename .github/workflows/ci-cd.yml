name: CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  REGISTRY: cr.yandex
  IMAGE_NAME: scribot-bot

jobs:
  lint:
    name: Run Linter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff
      
      - name: Run Ruff
        run: |
          ruff check .

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            poppler-utils \
            texlive-latex-base \
            texlive-latex-extra \
            texlive-fonts-recommended \
            texlive-lang-cyrillic
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
      
      - name: Run tests
        env:
          BOT_TOKEN: "test_token"
          CHAT_URL: "https://test.chat.url"
          FEEDBACK_URL: "https://test.feedback.url"
          SOS_URL: "https://test.sos.url"
          ADMIN_ID: "123456789"
          LLM_TOKEN: "test_llm_token"
        run: |
          pytest tests/ -v --tb=short || python -m unittest discover tests -v

  build-and-push:
    name: Build and Push Docker Image
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to Yandex Container Registry
        run: |
          # Сохраняем JSON ключ во временный файл
          echo '${{ secrets.YC_SA_JSON_CREDENTIALS }}' > /tmp/key.json
          # Логинимся используя docker
          cat /tmp/key.json | docker login \
            --username json_key \
            --password-stdin \
            cr.yandex/${{ secrets.YC_REGISTRY_ID }}
          # Удаляем временный файл
          rm /tmp/key.json
      
      - name: Build and push Docker image
        run: |
          IMAGE_TAG="${{ env.REGISTRY }}/${{ secrets.YC_REGISTRY_ID }}/${{ env.IMAGE_NAME }}"
          # Собираем образ
          docker build -t ${IMAGE_TAG}:latest -t ${IMAGE_TAG}:${{ github.sha }} -f deployment/Dockerfile .
          # Пушим оба тега
          docker push ${IMAGE_TAG}:latest
          docker push ${IMAGE_TAG}:${{ github.sha }}

  deploy:
    name: Deploy to Server
    needs: build-and-push
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.YC_INSTANCE_IP }} >> ~/.ssh/known_hosts
      
      - name: Copy docker-compose.prod.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.YC_INSTANCE_IP }}
          username: ${{ secrets.YC_INSTANCE_USER }}
          key_path: ~/.ssh/deploy_key
          source: "deployment/docker-compose.prod.yml"
          target: "~/scribot-bot/"
      
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_URL: ${{ secrets.CHAT_URL }}
          FEEDBACK_URL: ${{ secrets.FEEDBACK_URL }}
          SOS_URL: ${{ secrets.SOS_URL }}
          ADMIN_ID: ${{ secrets.ADMIN_ID }}
          LOG_LEVEL: ${{ secrets.LOG_LEVEL || 'all' }}
          REQUIRED_CHANNELS: ${{ secrets.REQUIRED_CHANNELS || '' }}
          LLM_TOKEN: ${{ secrets.LLM_TOKEN }}
          YC_REGISTRY_ID: ${{ secrets.YC_REGISTRY_ID }}
          NEW_IMAGE: ${{ env.REGISTRY }}/${{ secrets.YC_REGISTRY_ID }}/${{ env.IMAGE_NAME }}:latest
          BACKUP_IMAGE: ${{ env.REGISTRY }}/${{ secrets.YC_REGISTRY_ID }}/${{ env.IMAGE_NAME }}:stable
        with:
          host: ${{ secrets.YC_INSTANCE_IP }}
          username: ${{ secrets.YC_INSTANCE_USER }}
          key_path: ~/.ssh/deploy_key
          envs: BOT_TOKEN,CHAT_URL,FEEDBACK_URL,SOS_URL,ADMIN_ID,LOG_LEVEL,REQUIRED_CHANNELS,LLM_TOKEN,YC_REGISTRY_ID,NEW_IMAGE,BACKUP_IMAGE
          script: |
            set -e  # Остановка при ошибке
            
            echo "🔄 Начинаем деплой..."
            
            # Создаем директории если их нет
            export DEPLOY_DIR="$HOME/scribot-bot"
            mkdir -p "$DEPLOY_DIR/data" "$DEPLOY_DIR/logs"
            echo "📁 Директория деплоя: $DEPLOY_DIR"
            
            # Логинимся в Yandex Container Registry
            echo "🔐 Логинимся в Yandex Container Registry..."
            echo '${{ secrets.YC_SA_JSON_CREDENTIALS }}' > /tmp/key.json
            cat /tmp/key.json | docker login \
              --username json_key \
              --password-stdin \
              cr.yandex/${{ secrets.YC_REGISTRY_ID }}
            rm /tmp/key.json
            
            # Сохраняем информацию о текущем работающем контейнере
            CURRENT_IMAGE=""
            if docker ps --filter name=scribot_bot --format "{{.Image}}" | grep -q .; then
              CURRENT_IMAGE=$(docker ps --filter name=scribot_bot --format "{{.Image}}")
              echo "📸 Текущий образ: $CURRENT_IMAGE"
              # Тегируем текущий образ как stable (резервная копия)
              docker tag "$CURRENT_IMAGE" "$BACKUP_IMAGE" || echo "⚠️  Не удалось создать backup тег"
            else
              echo "⚠️  Текущий контейнер не найден"
            fi
            
            # Обновляем новый образ
            echo "⬇️  Скачиваем новый образ..."
            docker pull "$NEW_IMAGE"
            
            # Останавливаем и удаляем текущий контейнер
            echo "⏸️  Останавливаем текущий контейнер..."
            
            # Получаем ID всех контейнеров с именем scribot_bot (включая остановленные)
            CONTAINER_IDS=$(docker ps -a --filter name=scribot_bot --format "{{.ID}}" || true)
            
            if [ -n "$CONTAINER_IDS" ]; then
              echo "🗑️  Найдены контейнеры для удаления: $CONTAINER_IDS"
              for CONTAINER_ID in $CONTAINER_IDS; do
                echo "  Отключаем автоперезапуск для $CONTAINER_ID..."
                docker update --restart=no "$CONTAINER_ID" 2>/dev/null || true
                
                # Получаем PID процесса контейнера
                CONTAINER_PID=$(docker inspect "$CONTAINER_ID" --format '{{.State.Pid}}' 2>/dev/null || echo "")
                
                if [ -n "$CONTAINER_PID" ] && [ "$CONTAINER_PID" != "0" ]; then
                  echo "  Убиваем процесс контейнера $CONTAINER_ID (PID: $CONTAINER_PID)..."
                  sudo kill -9 "$CONTAINER_PID" 2>/dev/null || true
                  sleep 1
                else
                  echo "  Процесс контейнера $CONTAINER_ID не найден или уже остановлен"
                fi
                
                echo "  Удаляем контейнер $CONTAINER_ID..."
                docker rm -f "$CONTAINER_ID" 2>/dev/null || true
              done
              sleep 2
            fi
            
            # Финальная проверка
            if docker ps -a --filter name=scribot_bot --format "{{.Names}}" | grep -q scribot_bot; then
              echo "⚠️  Контейнер все еще существует, применяем экстренные меры..."
              
              # Последняя попытка - убить все процессы и принудительно удалить
              REMAINING_IDS=$(docker ps -a --filter name=scribot_bot --format "{{.ID}}")
              for CONTAINER_ID in $REMAINING_IDS; do
                CONTAINER_PID=$(docker inspect "$CONTAINER_ID" --format '{{.State.Pid}}' 2>/dev/null || echo "")
                if [ -n "$CONTAINER_PID" ] && [ "$CONTAINER_PID" != "0" ]; then
                  echo "  Экстренно убиваем PID: $CONTAINER_PID..."
                  sudo kill -9 "$CONTAINER_PID" 2>/dev/null || true
                fi
                sleep 1
                docker rm -f "$CONTAINER_ID" 2>/dev/null || true
              done
              sleep 2
              
              # Финальная-финальная проверка
              if docker ps -a --filter name=scribot_bot --format "{{.Names}}" | grep -q scribot_bot; then
                echo "❌ ОШИБКА: Контейнер все еще существует после всех попыток!"
                docker ps -a --filter name=scribot_bot
                exit 1
              fi
            fi
            
            echo "✅ Все старые контейнеры удалены"
            
            # Функция для запуска контейнера через docker-compose
            start_container() {
              echo "🚀 Запускаем контейнер через docker-compose..."
              
              # Убеждаемся что старый контейнер полностью убит
              OLD_CONTAINERS=$(docker ps -a --filter name=scribot_bot --format "{{.ID}}" || true)
              if [ -n "$OLD_CONTAINERS" ]; then
                echo "⚠️  Найдены остатки старых контейнеров, удаляем..."
                for CID in $OLD_CONTAINERS; do
                  # Убиваем процесс
                  OLD_PID=$(docker inspect "$CID" --format '{{.State.Pid}}' 2>/dev/null || echo "")
                  if [ -n "$OLD_PID" ] && [ "$OLD_PID" != "0" ]; then
                    echo "  Убиваем PID: $OLD_PID"
                    sudo kill -9 "$OLD_PID" 2>/dev/null || true
                  fi
                  sleep 1
                  # Удаляем контейнер
                  docker rm -f "$CID" 2>/dev/null || true
                done
                sleep 2
              fi
              
              # Создаем .env файл со всеми переменными окружения
              echo "📝 Создаем .env файл..."
              {
                echo "BOT_TOKEN=$BOT_TOKEN"
                echo "CHAT_URL=$CHAT_URL"
                echo "FEEDBACK_URL=$FEEDBACK_URL"
                echo "SOS_URL=$SOS_URL"
                echo "ADMIN_ID=$ADMIN_ID"
                echo "LOG_LEVEL=$LOG_LEVEL"
                echo "REQUIRED_CHANNELS=$REQUIRED_CHANNELS"
                echo "LLM_TOKEN=$LLM_TOKEN"
                echo "YC_REGISTRY_ID=$YC_REGISTRY_ID"
              } > "$DEPLOY_DIR/.env"
              
              # Запускаем через docker-compose
              cd "$DEPLOY_DIR"
              sudo docker-compose -f docker-compose.prod.yml up -d
            }
            
            # Запускаем новый контейнер
            echo "📦 Используем образ: $NEW_IMAGE"
            if start_container; then
              echo "✅ Контейнер запущен"
            else
              echo "❌ Ошибка запуска контейнера"
              exit 1
            fi
            
            # Ждем пока контейнер станет healthy (максимум 60 секунд)
            echo "⏳ Ожидаем запуска и проверки healthcheck (до 60 секунд)..."
            HEALTH_CHECK_TIMEOUT=60
            HEALTH_CHECK_INTERVAL=5
            ELAPSED=0
            
            while [ $ELAPSED -lt $HEALTH_CHECK_TIMEOUT ]; do
              HEALTH_STATUS=$(docker inspect --format='{{.State.Health.Status}}' scribot_bot 2>/dev/null || echo "none")
              
              if [ "$HEALTH_STATUS" = "healthy" ]; then
                echo "✅ Контейнер здоров (healthy)"
                break
              elif [ "$HEALTH_STATUS" = "none" ]; then
                # Контейнер без healthcheck или не запущен
                if docker ps --filter name=scribot_bot --filter status=running | grep -q scribot_bot; then
                  echo "ℹ️  Контейнер работает (healthcheck не настроен)"
                  break
                else
                  echo "❌ Контейнер не запущен"
                  break
                fi
              else
                echo "⏳ Статус: $HEALTH_STATUS (прошло ${ELAPSED}с)"
                sleep $HEALTH_CHECK_INTERVAL
                ELAPSED=$((ELAPSED + HEALTH_CHECK_INTERVAL))
              fi
            done
            
            # Финальная проверка статуса
            FINAL_HEALTH_STATUS=$(docker inspect --format='{{.State.Health.Status}}' scribot_bot 2>/dev/null || echo "none")
            CONTAINER_RUNNING=$(docker ps --filter name=scribot_bot --filter status=running | grep -q scribot_bot && echo "yes" || echo "no")
            
            if [ "$FINAL_HEALTH_STATUS" != "healthy" ] && [ "$FINAL_HEALTH_STATUS" != "none" ]; then
              echo "❌ Контейнер не прошел healthcheck за ${HEALTH_CHECK_TIMEOUT} секунд!"
              echo "Финальный статус: $FINAL_HEALTH_STATUS"
              docker logs scribot_bot --tail=50
              
              # Откатываемся к стабильной версии
              if [ -n "$CURRENT_IMAGE" ]; then
                echo "🔙 Откатываемся к предыдущей версии из-за проблем с healthcheck"
                
                # Получаем PID процесса и убиваем его
                CONTAINER_PID=$(docker inspect scribot_bot --format '{{.State.Pid}}' 2>/dev/null || echo "")
                if [ -n "$CONTAINER_PID" ] && [ "$CONTAINER_PID" != "0" ]; then
                  sudo kill -9 "$CONTAINER_PID" 2>/dev/null || true
                  sleep 1
                fi
                
                docker stop scribot_bot 2>/dev/null || true
                docker rm -f scribot_bot 2>/dev/null || true
                
                # Перетегируем backup образ как latest для docker-compose
                echo "📦 Перетегируем $BACKUP_IMAGE -> $NEW_IMAGE"
                docker tag "$BACKUP_IMAGE" "$NEW_IMAGE" || echo "⚠️ Не удалось перетегировать"
                
                start_container
                sleep 10
                echo "🔄 Откат выполнен. Проверьте логи:"
                docker logs scribot_bot --tail=30
                exit 1
              else
                echo "⚠️  Нет предыдущей версии для отката!"
                exit 1
              fi
            fi
            
            if [ "$CONTAINER_RUNNING" = "yes" ]; then
              echo "✅ Контейнер работает"
              
              # Проверяем логи на наличие критических ошибок
              LOGS=$(docker logs scribot_bot 2>&1 | tail -100)
              if echo "$LOGS" | grep -iE "CRITICAL|ERROR.*Failed to|Exception.*startup|❌"; then
                echo "❌ Обнаружены критические ошибки в логах!"
                
                # Откатываемся к стабильной версии
                if [ -n "$CURRENT_IMAGE" ]; then
                  echo "🔙 Откатываемся к предыдущей версии: $CURRENT_IMAGE"
                  
                  # Получаем PID процесса и убиваем его
                  CONTAINER_PID=$(docker inspect scribot_bot --format '{{.State.Pid}}' 2>/dev/null || echo "")
                  if [ -n "$CONTAINER_PID" ] && [ "$CONTAINER_PID" != "0" ]; then
                    sudo kill -9 "$CONTAINER_PID" 2>/dev/null || true
                    sleep 1
                  fi
                  
                  docker stop scribot_bot 2>/dev/null || true
                  docker rm -f scribot_bot 2>/dev/null || true
                  
                  # Перетегируем backup образ как latest для docker-compose
                  echo "📦 Перетегируем $BACKUP_IMAGE -> $NEW_IMAGE"
                  docker tag "$BACKUP_IMAGE" "$NEW_IMAGE" || echo "⚠️ Не удалось перетегировать"
                  
                  start_container
                  sleep 10
                  echo "🔄 Откат выполнен. Проверьте логи:"
                  docker logs scribot_bot --tail=30
                  exit 1
                else
                  echo "⚠️  Нет предыдущей версии для отката!"
                  docker logs scribot_bot --tail=50
                  exit 1
                fi
              else
                echo "✅ Деплой успешен!"
                docker logs scribot_bot --tail=30
                echo "📊 Статус контейнера:"
                docker ps --filter name=scribot_bot
              fi
            else
              echo "❌ Контейнер не работает!"
              
              # Откатываемся к стабильной версии
              if [ -n "$CURRENT_IMAGE" ]; then
                echo "🔙 Откатываемся к предыдущей версии: $CURRENT_IMAGE"
                
                # Получаем PID процесса и убиваем его
                CONTAINER_PID=$(docker inspect scribot_bot --format '{{.State.Pid}}' 2>/dev/null || echo "")
                if [ -n "$CONTAINER_PID" ] && [ "$CONTAINER_PID" != "0" ]; then
                  sudo kill -9 "$CONTAINER_PID" 2>/dev/null || true
                  sleep 1
                fi
                
                docker stop scribot_bot 2>/dev/null || true
                docker rm -f scribot_bot 2>/dev/null || true
                
                # Перетегируем backup образ как latest для docker-compose
                echo "📦 Перетегируем $BACKUP_IMAGE -> $NEW_IMAGE"
                docker tag "$BACKUP_IMAGE" "$NEW_IMAGE" || echo "⚠️ Не удалось перетегировать"
                
                start_container
                sleep 10
                echo "🔄 Откат выполнен"
                docker logs scribot_bot --tail=30
              else
                echo "⚠️  Нет предыдущей версии для отката!"
                docker logs scribot_bot --tail=50
              fi
              exit 1
            fi
      
      - name: Final verification
        if: always()
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.YC_INSTANCE_IP }}
          username: ${{ secrets.YC_INSTANCE_USER }}
          key_path: ~/.ssh/deploy_key
          script: |
            echo "🔍 Финальная проверка..."
            if docker ps --filter name=scribot_bot --filter status=running | grep -q scribot_bot; then
              echo "✅ Бот работает"
              echo ""
              echo "📊 Информация о контейнере:"
              docker ps --filter name=scribot_bot --format "table {{.Names}}\t{{.Status}}\t{{.Image}}"
              echo ""
              echo "📝 Последние логи:"
              docker logs scribot_bot --tail=20
            else
              echo "❌ ВНИМАНИЕ: Бот не работает!"
              echo ""
              echo "📝 Последние логи:"
              docker logs scribot_bot --tail=50 2>&1 || echo "Логи недоступны"
              exit 1
            fi

