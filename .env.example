# Конфигурация окружения для ScribotV2
# Скопируйте этот файл в .env и заполните значения

# ⚠️ ВАЖНО: При добавлении новой переменной окружения в core/settings.py
# обязательно обновите:
# 1. .github/workflows/ci-cd.yml - секции env в тестах (два места: Run tests и Test migrations)
# 2. .github/workflows/ci-cd.yml - секцию env в deploy job
# 3. .github/workflows/ci-cd.yml - список envs в deploy job
# Подробнее см. docs/CI_CD_VARIABLES.md

# Токен Telegram бота (обязательно)
BOT_TOKEN=your_bot_token_here

# URL канала для подписки (обязательно)
CHANNEL_URL=https://t.me/your_channel

# URL для обратной связи (обязательно)
FEEDBACK_URL=https://your_feedback_url

# URL для экстренной связи (обязательно)
SOS_URL=https://your_sos_url

# ID администратора бота (обязательно, числовое значение)
ADMIN_ID=123456789

# Токен для LLM API (обязательно)
LLM_TOKEN=your_llm_token_here

# Уровень логирования (опционально, по умолчанию: all)
# Возможные значения: all, none
LOG_LEVEL=all

# Обязательные каналы для подписки (опционально)
# Формат: @channel1,@channel2 или channel_id1,channel_id2
REQUIRED_CHANNELS=

# Базовая цена в звездочках Telegram (опционально, по умолчанию: 100)
BASE_PRICE=100

# Текст акции (опционально)
# Если указан, будет показываться в стартовом сообщении после /start
# Формат: текст акции (без префикса "действует акция:")
PROMOTION_TEXT=
